// 版本号
VERSION=0.0.1

// http服务端口
HTTP_PORT=3000

// 是否使用lowdb(true使用，false不使用)
USE_LOWDB=true

// token校验环境(true正式环境，false开发环境)
TOKEN_ENABLED=false

// token密钥
TOKEN_SECRET=test

// token过期时间
TOKEN_EXPIRES_IN=72h